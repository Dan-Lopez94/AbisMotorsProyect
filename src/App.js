import { Box, Heading } from "@chakra-ui/react";

function App() {
  return <Box className="no-select" align="center">
    <Heading color={"white"} textShadow="2px 2px 4px rgba(0, 0, 0, 0.4)">
      Bienvenidos a Abi's Motor's
    </Heading>
  </Box>;
}

export default App;
